name: Continuous Integration

on:
  push:
    branches-ignore:
      - main
      - 'releases/**'

env:
  BUILD_TYPE: Release

jobs:
  build_linux:
    name: "Linux"
    uses: ./.github/workflows/build.yml